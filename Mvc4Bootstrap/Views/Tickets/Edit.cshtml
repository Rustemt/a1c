@model A1fxCrm.Web.Models.CustomerTicket

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_LayoutBlue.cshtml";
}


<ul class="breadcrumb">
    <li><a href="#">Anasayfa</a> <span class="divider">/</span></li>
    <li class="active">Müşteriler -  @Model.Customer.FirstName @Model.Customer.LastName </li>
</ul>
 
@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
    
        @Html.HiddenFor(model => model.Id)

        <div class="editor-label">
            Not
        </div>
        <div class="editor-field">
            @Html.TextAreaFor(model => model.Content, new { @cols = "400", @rows = "3", @style = "margin: 0px 0px 10px; width: 654px; height: 69px;" })
            @Html.ValidationMessageFor(model => model.Content)
        </div>

        
 
        <div class="editor-field" style="visibility: hidden;">>
            @Html.DropDownList("CustomerId", String.Empty)
            @Html.ValidationMessageFor(model => model.CustomerId)
        </div>

        <p>
            <input type="submit" class="btn btn-primary" value="Kaydet" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Müşteri Detayı", "Edits", "Customer", new { id = @Model.Customer.Id }, new { })
</div>

@section Scripts {
    <script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
    <script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>
}
